/**
 * Arrays, tables, variables, dimensions for netcdf files.
 *
 * <p>*Definition classes are to define the structure and metadata of netcdf data.
 *
 * <p>The only other thing here is the NumericalArray which is there to contain multidimensional
 * numerical data that can be written to a Dimensional Variable.
 */
package org.fairdatapipeline.objects;
